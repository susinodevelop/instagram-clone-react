/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com', 'via.placeholder.com', 'img.freepik.com'], /*TODO revisar */
    },
};

export default nextConfig;
